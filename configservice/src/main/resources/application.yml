server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/johnna-endure/config_exam
          search-paths: webclient
        encrypt:
          enabled: false
